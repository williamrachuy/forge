package forge.ai.ultron;

// Scratch debug file used during TICKET-V3-205 development to compare
// GameStateEvaluator scores with/without a block assignment. No longer needed --
// left as an intentionally empty, no-op placeholder because file deletion tools were
// unavailable in this session's sandbox. Safe to delete in a future session.
public final class DebugBlockScoreTest {
    private DebugBlockScoreTest() {}
}
